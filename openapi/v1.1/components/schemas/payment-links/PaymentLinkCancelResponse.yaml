type: object
additionalProperties: false
properties:
  success: { type: boolean, example: true }
  message: { type: string, example: "Payment link canceled." }
  data: { $ref: ./PaymentLink.yaml }
required: [success, message, data]