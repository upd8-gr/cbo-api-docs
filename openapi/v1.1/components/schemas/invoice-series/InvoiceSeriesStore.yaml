type: object
additionalProperties: false
properties:
  name: { type: string, maxLength: 255, example: "Retail Series" }
  description: { type: string, nullable: true, maxLength: 500, example: "Main retail series" }
  visible: { type: boolean, nullable: true, example: true }
required: [name]
