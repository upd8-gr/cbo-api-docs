type: object
additionalProperties: false
properties:
  name: { type: string, maxLength: 255, example: "Retail Series Updated" }
  description: { type: string, nullable: true, maxLength: 500, example: "Updated description" }
  visible: { type: boolean, nullable: true, example: true }
