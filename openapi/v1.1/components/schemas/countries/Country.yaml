type: object
additionalProperties: false
properties:
  id: { type: integer, format: int64, example: 1 }
  name: { type: string, example: "Greece" }
  code: { type: string, example: "GR" }
required: [id, name, code]