type: object
additionalProperties: false
properties:
  data:
    type: array
    items: { $ref: ./Group.yaml }
  links:
    $ref: ../common/PaginationLinks.yaml
  meta:
    $ref: ../common/PaginationMeta.yaml
required: [data, links, meta]