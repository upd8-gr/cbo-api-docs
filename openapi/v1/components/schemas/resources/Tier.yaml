type: object
additionalProperties: true
description: Minimal placeholder for TierResource output.
properties:
  id: { type: integer, format: int64, example: 1 }
  title: { type: string, example: "Tier 1" }
