type: object
additionalProperties: true
description: Minimal placeholder for CharCategoryResource output.
properties:
  id: { type: integer, format: int64, example: 1 }
  title: { type: string, example: "Category A" }
  characteristics:
    type: array
    items:
      $ref: ./Characteristic.yaml
